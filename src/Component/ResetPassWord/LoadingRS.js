import React from 'react'
import { CommonLoading } from 'react-loadingg';

export const loadingRS = () => {
    return (
        <div className="loading-fixed">
            <CommonLoading />
        </div>
    );
}